# 콘텐츠 암호화 API

## 📖 개요
이 프로젝트는 REST API를 통해 콘텐츠를 안전하게 제공하는 시스템을 실증합니다. **AES-256-GCM**을 사용하여 콘텐츠를 보호하고 **ECDH (P-256)**를 통해 안전하게 키를 교환하며, 인증된 클라이언트에게 도달할 때까지 콘텐츠가 암호화된 상태로 유지되도록 보장합니다.

## 🚀 주요 기능
- **안전한 키 교환**: P-256 곡선에서 타원 곡선 디피-헬먼(ECDH)을 사용합니다.
- **강력한 암호화**: 콘텐츠는 AES-256 GCM 모드를 사용하여 암호화됩니다.
- **클라이언트 인증**: 타임스탬프와 솔트(Salt)가 포함된 HMAC-SHA256 서명을 사용하여 클라이언트를 검증하고 재전송 공격을 방지합니다.
- **크로스 플랫폼**: 다음 플랫폼에 대한 참조 구현을 포함합니다:
  - **백엔드**: Spring Boot 3.5 (Java 21)
  - **웹**: Web Crypto API 및 Wasm을 사용하는 Vanilla JS
  - **iOS**: Swift (CryptoKit)
  - **Android**: Kotlin

## 🛠 기술 스택
| 컴포넌트 | 기술 | 사용 사례 |
|-----------|------------|----------|
| **Backend** | Java 21, Spring Boot 3.5 | API 서버, 암호화 로직 |
| **Web** | HTML5, JavaScript, WebAssembly | 브라우저 기반 복호화 |
| **iOS** | Swift, CryptoKit | 네이티브 iOS 클라이언트 |
| **Android** | Kotlin, javax.crypto | 네이티브 Android 클라이언트 |

## 🔐 암호화 프로토콜
이 시스템은 견고한 4단계 핸드셰이크 및 암호화 프로세스를 사용합니다:

1.  **핸드셰이크 요청**: 클라이언트는 공유 비밀(HMAC)로 서명된 자신의 ECDH 공개 키를 전송합니다.
    - 페이로드: `PublicKey`, `Timestamp`, `Signature`, `Salt`.
2.  **검증**: 서버는 서명을 검증하고 타임스탬프를 확인합니다(5분 이내여야 함).
3.  **키 유도**:
    - ECDH를 통해 `공유 비밀(Shared Secret)`을 계산합니다.
    - 제공된 `Salt`와 컨텍스트 정보를 사용하여 **HKDF-SHA256**으로 `세션 키(Session Key)`를 유도합니다.
4.  **콘텐츠 전송**: 서버는 암호화된 콘텐츠(AES-256-GCM)와 서버의 공개 키로 응답합니다.

상세한 암호화 명세는 [ENCRYPTION_SPECS.md](ENCRYPTION_SPECS.md)를 참조하십시오.

## 🚀 실행 방법

### 1. Backend (Java/Spring Boot)
API 서버를 먼저 실행해야 합니다.

```bash
cd backend
./gradlew bootRun
```
*   서버는 기본적으로 `http://localhost:8080`에서 실행됩니다.
*   API 엔드포인트: `POST /api/novels/{id}`

### 2. Web Client
별도의 빌드 없이 브라우저에서 바로 파일을 열어 테스트할 수 있습니다. (CORS 설정이 `*`로 되어 있어 로컬 파일 실행 가능)

1.  `web/index.html` 파일을 브라우저(Chrome, Safari 등)로 엽니다.
2.  화면의 버튼을 클릭하여 키 교환 및 복호화 과정을 확인합니다.

### 3. iOS Client (NovelApp)
1.  `ios/NovelApp` 폴더를 Xcode로 엽니다.
2.  `CryptoManager.swift`의 URL이 로컬 서버(`localhost`)를 가리키고 있는지 확인합니다.
    *   *주의: 시뮬레이터에서는 `localhost`가 호스트 PC를 가리키지만, 실제 기기에서는 PC의 내부 IP(예: `192.168.x.x`)로 변경해야 합니다.*
3.  앱을 빌드하고 실행합니다.

### 4. Android Client
1.  `android` 폴더를 Android Studio로 엽니다.
2.  `CryptoManager.kt` 또는 통신 설정에서 서버 URL을 확인합니다.
    *   *에뮬레이터 사용 시 `10.0.2.2`가 호스트 PC의 `localhost`입니다.*
3.  앱을 빌드하고 실행합니다.

---

## ⚠️ 보안 주의사항 (Security Notice)

이 예제 코드는 학습 및 데모 목적으로 작성되었습니다. 실제 서비스 도입 시 다음 사항을 반드시 보완해야 합니다.

1.  **Client Secret 관리**:
    *   현재 코드에는 클라이언트 인증을 위해 `auth-secret-1234`가 하드코딩되어 있습니다.
    *   **개선 필요**: 실제 환경에서는 앱 내에 Secret을 저장하지 말고, 사용자 로그인 후 발급받은 **일회성 세션 토큰** 등을 사용하여 인증해야 합니다.
2.  **HTTPS 사용**:
    *   모든 통신은 반드시 HTTPS(TLS) 위에서 이루어져야 합니다.
3.  **예외 처리**:
    *   보안 관련 에러(복호화 실패 등) 발생 시, 공격자에게 상세한 정보를 노출하지 않도록 일반적인 에러 메시지만 반환해야 합니다.

